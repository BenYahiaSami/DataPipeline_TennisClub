This project works without any special requirements aside from the lxml library, which must be installed for the scripts to run properly.

To do so, navigate to the appropriate folder in your terminal, activate your virtual environment, and run on your terminal:

pip install -r requirements.txt